# project setting

### 二维码存放目录 (文件夹)
qrcode.image.path=${user.dir}/img

### 扫码等待时长 (单位:秒)
scan.waitTime=10

### 重新脚本命令
restart.cmd=/root/project/clean/bin/local_run.sh restart

### thymeleaf
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html

# 日志
logging.file.path=logs